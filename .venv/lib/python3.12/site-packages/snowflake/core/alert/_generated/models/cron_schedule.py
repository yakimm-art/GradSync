
"""
Snowflake Alert API

The Snowflake Alert API is a REST API that you can use to access, update, and perform certain actions on Alert resource in a Snowflake database.  # noqa: E501

The version of the OpenAPI document: 0.0.1
Contact: support@snowflake.com
Generated by: https://openapi-generator.tech

Do not edit this file manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401

from typing import Any

from pydantic import StrictStr

from snowflake.core.alert._generated.models.schedule import Schedule


class CronSchedule(Schedule):
    """A model object representing the CronSchedule resource.

    Constructs an object of type CronSchedule with the provided properties.

    Parameters
    __________
    cron_expr : str
        A cron expression for the scheduled execution.
    timezone : str
        The time zone for the schedule.
    """

    cron_expr: StrictStr

    timezone: StrictStr

    __properties = ["schedule_type"]

    class Config:
        populate_by_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias."""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias."""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> CronSchedule:
        """Create an instance of CronSchedule from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(
        self,
        hide_readonly_properties: bool = False,
    ) -> dict[str, Any]:
        """Returns the dictionary representation of the model using alias"""
        exclude_properties = set()

        if hide_readonly_properties:
            exclude_properties.update({})

        _dict = dict(self._iter(to_dict=True, by_alias=True, exclude=exclude_properties, exclude_none=True))

        _dict["schedule_type"] = Schedule.get_child_model_discriminator_value("CronSchedule")

        return _dict

    def to_dict_without_readonly_properties(self) -> dict[str, Any]:
        """Return the dictionary representation of the model without readonly properties."""
        return self.to_dict(hide_readonly_properties=True)

    @classmethod
    def from_dict(cls, obj: dict) -> CronSchedule:
        """Create an instance of CronSchedule from a dict."""
        if obj is None:
            return None

        if type(obj) is not dict:
            return CronSchedule.parse_obj(obj)

        _obj = CronSchedule.parse_obj(
            {
                "cron_expr": obj.get("cron_expr"),
                "timezone": obj.get("timezone"),
            }
        )

        return _obj


class CronScheduleModel(Schedule):
    def __init__(
        self,
        cron_expr: str,
        timezone: str,
        # optional properties
    ):
        """A model object representing the CronSchedule resource.

        Constructs an object of type CronSchedule with the provided properties.

        Parameters
        __________
        cron_expr : str
            A cron expression for the scheduled execution.
        timezone : str
            The time zone for the schedule.
        """
        super().__init__()
        self.cron_expr = cron_expr
        self.timezone = timezone

    __properties = ["schedule_type"]

    def __repr__(self) -> str:
        return repr(self._to_model())

    def _to_model(self):
        return CronSchedule(
            cron_expr=self.cron_expr,
            timezone=self.timezone,
        )

    @classmethod
    def _from_model(cls, model) -> CronScheduleModel:
        return CronScheduleModel(
            cron_expr=model.cron_expr,
            timezone=model.timezone,
        )

    def to_dict(self):
        """Create a dictionary of the properties from a CronSchedule.

        This method constructs a dictionary with the key-value entries corresponding to the properties of the CronSchedule object.

        Returns
        _______
        dict
            A dictionary object created using the input model.
        """
        return self._to_model().to_dict()

    @classmethod
    def from_dict(cls, obj: dict) -> CronScheduleModel:
        """Create an instance of CronSchedule from a dict.

        This method constructs a CronSchedule object from a dictionary with the key-value pairs of its properties.

        Parameters
        ----------
        obj : dict
            A dictionary whose keys and values correspond to the properties of the resource object.

        Returns
        _______
        CronSchedule
            A CronSchedule object created using the input dictionary; this will fail if the required properties are missing.
        """
        return cls._from_model(CronSchedule.from_dict(obj))


CronSchedule._model_class = CronScheduleModel
