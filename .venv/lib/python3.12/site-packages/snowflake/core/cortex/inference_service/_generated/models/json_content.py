
"""
Cortex Inference API

OpenAPI 3.0 specification for the Cortex REST API  # noqa: E501

The version of the OpenAPI document: 0.1.0
Contact: support@snowflake.com
Generated by: https://openapi-generator.tech

Do not edit this file manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401

from typing import Any, Dict

from pydantic import BaseModel, Field, StrictStr, field_validator


class JSONContent(BaseModel):
    """A model object representing the JSONContent resource.

    Constructs an object of type JSONContent with the provided properties.

    Parameters
    __________
    type : str
        Identifies this as JSON content
    var_json : object
        Arbitrary JSON data structure
    """

    type: StrictStr

    var_json: Dict[str, Any] = Field(alias="json")

    __properties = ["type", "json"]

    @field_validator("type")
    def type_validate_enum(cls, v):
        if v not in ("json"):
            raise ValueError("must validate the enum values ('json')")
        return v

    class Config:
        populate_by_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias."""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias."""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> JSONContent:
        """Create an instance of JSONContent from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(
        self,
        hide_readonly_properties: bool = False,
    ) -> dict[str, Any]:
        """Returns the dictionary representation of the model using alias"""
        exclude_properties = set()

        if hide_readonly_properties:
            exclude_properties.update({})

        _dict = dict(self._iter(to_dict=True, by_alias=True, exclude=exclude_properties, exclude_none=True))

        return _dict

    def to_dict_without_readonly_properties(self) -> dict[str, Any]:
        """Return the dictionary representation of the model without readonly properties."""
        return self.to_dict(hide_readonly_properties=True)

    @classmethod
    def from_dict(cls, obj: dict) -> JSONContent:
        """Create an instance of JSONContent from a dict."""
        if obj is None:
            return None

        if type(obj) is not dict:
            return JSONContent.parse_obj(obj)

        _obj = JSONContent.parse_obj(
            {
                "type": obj.get("type"),
                "var_json": obj.get("json"),
            }
        )

        return _obj


class JSONContentModel:
    def __init__(
        self,
        type: str,
        var_json: object,
        # optional properties
    ):
        """A model object representing the JSONContent resource.

        Constructs an object of type JSONContent with the provided properties.

        Parameters
        __________
        type : str
            Identifies this as JSON content
        var_json : object
            Arbitrary JSON data structure
        """
        self.type = type
        self.var_json = var_json

    __properties = ["type", "json"]

    def __repr__(self) -> str:
        return repr(self._to_model())

    def _to_model(self):
        return JSONContent(
            type=self.type,
            var_json=self.var_json,
        )

    @classmethod
    def _from_model(cls, model) -> JSONContentModel:
        return JSONContentModel(
            type=model.type,
            var_json=model.var_json,
        )

    def to_dict(self):
        """Create a dictionary of the properties from a JSONContent.

        This method constructs a dictionary with the key-value entries corresponding to the properties of the JSONContent object.

        Returns
        _______
        dict
            A dictionary object created using the input model.
        """
        return self._to_model().to_dict()

    @classmethod
    def from_dict(cls, obj: dict) -> JSONContentModel:
        """Create an instance of JSONContent from a dict.

        This method constructs a JSONContent object from a dictionary with the key-value pairs of its properties.

        Parameters
        ----------
        obj : dict
            A dictionary whose keys and values correspond to the properties of the resource object.

        Returns
        _______
        JSONContent
            A JSONContent object created using the input dictionary; this will fail if the required properties are missing.
        """
        return cls._from_model(JSONContent.from_dict(obj))


JSONContent._model_class = JSONContentModel
